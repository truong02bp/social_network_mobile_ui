import 'package:flutter/material.dart';

class VideoNetworkScreen extends StatelessWidget {
  const VideoNetworkScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('ddd'),
      ),
    );
  }
}
